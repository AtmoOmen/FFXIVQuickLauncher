* 修复了软件长时间位于后台时，再召回时会导致渲染区域异常的问题。
* 修复了账号数据库在无法打开时会被直接清空的问题，现在会保留原文件，仅在数据库文件确实损坏时备份后重建。
* 补充了云同步工具的注意事项：请勿将安装目录与数据目录纳入 OneDrive、群晖 Drive 等工具的同步范围。

> 当前分支仅支持 `Windows 11` 的 `x64` 环境，不提供 `Linux`、`macOS` 与 `SteamDeck` 等环境的相关支持
